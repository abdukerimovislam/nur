import 'package:flutter/material.dart';

class AppColors {
  // Глубокий изумрудный (Night Forest) — почти черный, очень благородный фон
  static const Color background = Color(0xFF06140F);

  // Карточки (Surface) — насыщенный темный изумруд, чтобы они слегка выделялись
  static const Color surface = Color(0xFF0F291E);

  // Основной акцент — Сусальное Золото (Premium Gold). Идеально для кнопок и свечения
  static const Color primary = Color(0xFFE5C158);

  // Вторичный акцент — Приглушенный шалфейный (Sage Green) для иконок и неактивных элементов
  static const Color secondary = Color(0xFF789484);

  // Тексты
  static const Color textPrimary = Color(0xFFFFFFFF); // Чистый белый для заголовков
  static const Color textSecondary = Color(0xFF9EBAAA); // Мягкий зеленовато-серый для описаний
}