import 'package:flutter/material.dart';

class AppColors {
  // Глубокий индиго/пурпурный (Night Sky)
  static const Color background = Color(0xFF0F0B1A);
  // Карточки с легким аметистовым оттенком
  static const Color surface = Color(0xFF1A142A);

  // Основной акцент — Розовое Золото (Rose Gold)
  static const Color primary = Color(0xFFDDA7A5);
  // Вторичный акцент — Лавандовый/Пепельный
  static const Color secondary = Color(0xFF7D7494);

  static const Color textPrimary = Color(0xFFF7F5FA);
  static const Color textSecondary = Color(0xFF8A829D);
}