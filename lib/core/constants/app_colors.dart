import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Nur Core Palette
  // Deep emerald background: calm, premium, Islamic aesthetic.
  static const Color background = Color(0xFF06140F);

  // Elevated dark surface for cards, sheets and containers.
  static const Color surface = Color(0xFF0F291E);

  // Softer secondary surface for nested cards.
  static const Color surfaceAlt = Color(0xFF143527);

  // Premium gold accent for important actions.
  static const Color primary = Color(0xFFE5C158);

  // Deep emerald accent for positive states.
  static const Color emerald = Color(0xFF16A071);

  // Sage green for secondary icons/text.
  static const Color secondary = Color(0xFF789484);

  // Spiritual warm beige for light text highlights.
  static const Color cream = Color(0xFFF7EFD9);

  // Semantic colors.
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF38BDF8);

  // Text colors.
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9EBAAA);
  static const Color textMuted = Color(0xFF6F897B);

  // Borders and dividers.
  static const Color border = Color(0x1FFFFFFF);
  static const Color divider = Color(0x14FFFFFF);

  // Premium glow overlays.
  static const Color goldGlow = Color(0x33E5C158);
  static const Color emeraldGlow = Color(0x3316A071);
}